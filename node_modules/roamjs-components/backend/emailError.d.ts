import type { AxiosError } from "axios";
import React from "react";
declare const emailError: (subject: string, e: AxiosError | Error, moreBody?: React.ReactNode) => Promise<string>;
export default emailError;

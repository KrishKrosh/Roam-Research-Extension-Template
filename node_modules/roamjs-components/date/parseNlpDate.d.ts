import * as chrono from "chrono-node";
export declare const addNlpDateParser: (p: chrono.Parser) => number;
declare const parseRoamDate: (s: string, ref?: Date | undefined) => Date;
export default parseRoamDate;

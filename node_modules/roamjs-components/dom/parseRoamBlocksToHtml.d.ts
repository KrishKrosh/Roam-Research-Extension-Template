import { RoamContext } from "../marked";
import type { TreeNode, ViewType } from "../types";
declare const parseRoamBlocksToHtml: ({ content, viewType, level, context, }: {
    level: number;
    context: Required<RoamContext>;
    content: TreeNode[];
    viewType: ViewType;
}) => string;
export default parseRoamBlocksToHtml;

"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const getPageTitleByHtmlElement_1 = tslib_1.__importDefault(require("./getPageTitleByHtmlElement"));
const getPageTitleValueByHtmlElement = (e) => {
    const elToTitle = (e) => {
        if (!e) {
            return "";
        }
        else if (e.nodeName === "#text") {
            return e.nodeValue || "";
        }
        else if (e.nodeName === "SPAN" &&
            e.classList.contains("rm-page-ref__brackets")) {
            return "";
        }
        else if (e.nodeName === "SPAN" &&
            e.classList.contains("rm-page-ref")) {
            return `[[${Array.from(e.childNodes).map(elToTitle).join("")}]]`;
        }
        else {
            return Array.from(e.childNodes).map(elToTitle).join("");
        }
    };
    return elToTitle((0, getPageTitleByHtmlElement_1.default)(e));
};
exports.default = getPageTitleValueByHtmlElement;
//# sourceMappingURL=getPageTitleValueByHtmlElement.js.map
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const openBlockElement = (block, position) => {
    if (block) {
        block.dispatchEvent(new MouseEvent("mousedown", { bubbles: true }));
        setTimeout(() => {
            const textArea = document.getElementById(block.id);
            if ((textArea === null || textArea === void 0 ? void 0 : textArea.tagName) === "TEXTAREA") {
                const selection = typeof position !== "number" ? textArea.value.length : position;
                textArea.dispatchEvent(new MouseEvent("mouseup", { bubbles: true }));
                textArea.setSelectionRange(selection, selection);
            }
        }, 50);
    }
};
exports.default = openBlockElement;
//# sourceMappingURL=openBlockElement.js.map
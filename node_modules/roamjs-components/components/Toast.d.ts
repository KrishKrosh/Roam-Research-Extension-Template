import { Intent, ToasterPosition } from "@blueprintjs/core";
declare type ToastProps = {
    id: string;
    content?: string;
    timeout?: number;
    intent?: Intent;
    position?: ToasterPosition;
};
export declare const render: ({ position, ...props }: ToastProps) => (() => void);
export default render;
